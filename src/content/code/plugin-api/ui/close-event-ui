// @ts-nocheck

// Register a listener to log every time the UI is closed
reearth.ui.on("close", () => {
  console.log("The UI was closed.");
});

// Register a one-time listener for the "close" event
reearth.ui.on(
  "close",
  () => {
    console.log("The UI closed - one-time listener executed.");
  },
  { once: true }
);
